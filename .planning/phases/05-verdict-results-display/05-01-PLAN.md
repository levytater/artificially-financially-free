---
phase: 05-verdict-results-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/formatting.ts
  - src/lib/verdict.ts
  - src/components/results/verdict-card.tsx
  - src/components/results/summary-cards.tsx
  - src/components/results/chart-tabs.tsx
  - src/components/results/theme-toggle.tsx
  - src/components/results/cost-breakdown-table.tsx
  - src/components/results/year-comparison-panel.tsx
  - src/components/layout/main-content.tsx
  - src/app/page.tsx
  - src/app/layout.tsx
  - src/components/ui/tabs.tsx
autonomous: true
requirements: [VIZ-01, VIZ-02, VIZ-07, VIZ-08]

must_haves:
  truths:
    - "User sees a clear 'Renting Comes Out Ahead' or 'Buying Comes Out Ahead' verdict with the dollar advantage and templated reasoning text"
    - "User sees 4 summary cards (rent net worth, buy net worth, dollar advantage, percentage advantage) in a 2x2 grid with dollar advantage visually emphasized"
    - "User sees 3-tab structure (Net Worth, Cash Flow, Investment Growth) with placeholder content for Phase 6 charts"
    - "User can toggle between light and dark themes via a button in the header"
    - "User sees a cost breakdown table with expandable rows for Initial Costs, Recurring Costs, Opportunity Costs, and Net Proceeds"
    - "User can slide a year selector from Year 1 to final year and see renter vs buyer metrics for that specific year"
    - "User can jump to break-even year and mortgage-paid-off year via preset buttons"
    - "When renter and buyer net worth are within 5%, verdict shows tie messaging"
  artifacts:
    - path: "src/lib/verdict.ts"
      provides: "Verdict computation logic from ComparisonResult"
      exports: ["calculateVerdict", "VerdictResult"]
    - path: "src/components/results/verdict-card.tsx"
      provides: "Verdict display card with templated reasoning"
      exports: ["VerdictCard"]
    - path: "src/components/results/summary-cards.tsx"
      provides: "2x2 summary metric cards grid"
      exports: ["SummaryCards"]
    - path: "src/components/results/chart-tabs.tsx"
      provides: "3-tab structure with Phase 6 chart placeholders"
      exports: ["ChartTabs"]
    - path: "src/components/results/theme-toggle.tsx"
      provides: "Light/dark theme toggle button"
      exports: ["ThemeToggle"]
    - path: "src/components/results/cost-breakdown-table.tsx"
      provides: "Expandable cost breakdown with Accordion"
      exports: ["CostBreakdownTable"]
    - path: "src/components/results/year-comparison-panel.tsx"
      provides: "Year slider with preset buttons and metrics"
      exports: ["YearComparisonPanel"]
    - path: "src/components/layout/main-content.tsx"
      provides: "Full results layout replacing all placeholders"
  key_links:
    - from: "src/components/results/verdict-card.tsx"
      to: "src/lib/verdict.ts"
      via: "import calculateVerdict"
      pattern: "calculateVerdict\\("
    - from: "src/components/layout/main-content.tsx"
      to: "src/hooks/use-debounced-comparison.ts"
      via: "useDebouncedComparison hook provides ComparisonResult"
      pattern: "useDebouncedComparison"
    - from: "src/components/results/verdict-card.tsx"
      to: "src/content/verdict-text.ts"
      via: "import verdict templates"
      pattern: "verdictText"
    - from: "src/components/results/year-comparison-panel.tsx"
      to: "ComparisonResult.yearlyComparison"
      via: "indexed by selected year"
      pattern: "yearlyComparison\\[.*\\]"
    - from: "src/components/results/cost-breakdown-table.tsx"
      to: "ComparisonResult.housingProjection"
      via: "upfrontCosts, yearlyProjection, exitPosition"
      pattern: "housingProjection"
---

<objective>
Build all Phase 5 result display components and wire them into the main content area, replacing all placeholder blocks with functional rent-vs-buy results.

Purpose: Users need to see a clear verdict, supporting metrics, detailed cost breakdowns, and year-by-year comparisons after entering their calculator inputs. This phase transforms the calculator from a data-processing tool into a user-facing decision-support interface.

Output: 7 new files (verdict logic, 6 UI components) + 4 modified files (formatting, main-content, page, layout). All placeholders replaced with live data-driven components consuming ComparisonResult from the debounced comparison hook.
</objective>

<execution_context>
@C:/Users/levfe/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/levfe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-verdict-results-display/05-CONTEXT.md
@.planning/phases/05-verdict-results-display/05-RESEARCH.md
@.planning/phases/04-input-panel-state-management/04-04-SUMMARY.md

Key source files to reference:
@src/types/investment.ts (ComparisonResult, YearlyComparison interfaces)
@src/types/housing.ts (HousingProjection, UpfrontCosts, ExitPosition, YearlyHousingCost interfaces)
@src/hooks/use-debounced-comparison.ts (provides ComparisonResult to MainContent)
@src/content/verdict-text.ts (verdict template strings with {{placeholders}})
@src/lib/formatting.ts (existing formatCurrency, formatPercentage utilities)
@src/components/layout/main-content.tsx (current placeholder layout to replace)
@src/app/page.tsx (header where theme toggle goes)
@src/app/layout.tsx (ThemeProvider config to update)
@src/components/ui/accordion.tsx (existing Accordion component for cost breakdown)
@src/components/ui/slider.tsx (existing Slider component for year selector)
@src/components/ui/card.tsx (existing Card component for summary cards)
@src/app/globals.css (light + dark theme OKLCH tokens already defined)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verdict logic, formatting utils, VerdictCard, SummaryCards, ChartTabs, ThemeToggle</name>
  <files>
    src/lib/formatting.ts
    src/lib/verdict.ts
    src/components/results/verdict-card.tsx
    src/components/results/summary-cards.tsx
    src/components/results/chart-tabs.tsx
    src/components/results/theme-toggle.tsx
    src/components/ui/tabs.tsx
  </files>
  <action>
**Step 1: Install shadcn/ui Tabs component**

Run `npx shadcn@latest add tabs` to generate `src/components/ui/tabs.tsx`. This is needed for ChartTabs.

**Step 2: Extend src/lib/formatting.ts**

Add two new utility functions to the EXISTING formatting.ts file (do NOT create a new file):
- `formatCurrencyDecimal(value: Decimal, compact?: boolean): string` — Accepts Decimal, calls `.toNumber()` at the display boundary, uses Intl.NumberFormat with en-CA locale, CAD currency, 0 fraction digits. When `compact=true`, use `notation: 'compact', compactDisplay: 'short'` (produces "$1.2M" for large values).
- `formatPercentageDecimal(value: Decimal, decimals?: number): string` — Accepts Decimal, formats as "X.X%" with configurable decimal places (default 1).

Keep ALL existing functions unchanged. Import Decimal type from decimal.js.

**Step 3: Create src/lib/verdict.ts**

Create verdict computation logic:

```typescript
import Decimal from 'decimal.js'
import type { ComparisonResult } from '@/types/investment'

export interface VerdictResult {
  decision: 'rent' | 'buy' | 'tie'
  advantage: Decimal          // absolute dollar difference (always positive)
  percentAdvantage: Decimal   // percentage difference (always positive)
  renterFinal: Decimal
  buyerFinal: Decimal
  breakEvenYear: number | 'never'
  timeHorizon: number
}

/** Tie threshold: outcomes within 5% are considered a tie */
const TIE_THRESHOLD = new Decimal('0.05')
```

Implement `calculateVerdict(results: ComparisonResult): VerdictResult`:
- Extract final year from `results.yearlyComparison[results.yearlyComparison.length - 1]`
- Compare `renterNetWorth` vs `buyerNetWorthWithSelling` (with selling costs, per user decision)
- Calculate absolute difference and percentage difference
- If percentage difference <= 5% (TIE_THRESHOLD), decision = 'tie'
- Otherwise, whichever is larger wins
- Include `breakEvenWithSelling` as breakEvenYear
- Include timeHorizon from yearlyComparison length

**Step 4: Create src/components/results/verdict-card.tsx**

'use client' component accepting `results: ComparisonResult` prop.

- Import `calculateVerdict` from `@/lib/verdict`
- Import `verdictText` from `@/content/verdict-text`
- Import `formatCurrencyDecimal` from `@/lib/formatting`
- Use `useMemo` to compute verdict from results
- Render as a Card component (shadcn/ui) with:
  - Subtle top border colored by verdict: green-ish for "buy wins", blue/purple for "rent wins", neutral/amber for tie
  - Headline text from verdictText[scenario].headline (e.g., "Renting Comes Out Ahead")
  - Summary paragraph: Replace {{placeholders}} in verdictText[scenario].summary with actual formatted values:
    - `{{timeHorizon}}` -> verdict.timeHorizon
    - `{{advantage}}` -> formatCurrencyDecimal(verdict.advantage)
    - `{{renterNetWorth}}` -> formatCurrencyDecimal(verdict.renterFinal)
    - `{{buyerNetWorth}}` -> formatCurrencyDecimal(verdict.buyerFinal)
  - Break-even year mentioned in text (per locked decision: NOT a separate card)
    - If break-even exists: "Buying becomes the better choice starting in Year X."
    - If 'never': "Buying never catches up within your time horizon."
  - For 'tie' scenario: Create a tie template inline since verdictText only has rent-wins and buy-wins. Message: "Over X years, renting and buying produce nearly identical outcomes — within Y% of each other. Non-financial factors (stability, flexibility, lifestyle) should guide your decision."
  - Display card as panel (NOT hero headline, per locked decision)
  - Use text-sm for body, text-lg font-semibold for headline

**Step 5: Create src/components/results/summary-cards.tsx**

'use client' component accepting `results: ComparisonResult` prop.

- Import Card from shadcn/ui, formatCurrencyDecimal from formatting
- Compute from final year of yearlyComparison:
  - Rent final net worth
  - Buy final net worth (withSelling)
  - Dollar advantage (absolute difference)
  - Percentage advantage
- Render 2x2 grid: `grid grid-cols-2 gap-4`
- Each card: Card component with label (text-xs text-muted-foreground uppercase tracking-wide) and value (text-xl font-bold)
- Dollar advantage card visually emphasized (per locked decision): Use `border-primary` border or `bg-primary/5` background to make it stand out. Larger font (text-2xl) on the value. Show which side wins as subtitle text.
- Use compact currency formatting for values over $100K
- Cards positioned BELOW tabs (per locked decision) — positioning handled in main-content.tsx Task 2

**Step 6: Create src/components/results/chart-tabs.tsx**

'use client' component (no props needed — purely structural for Phase 6).

- Import Tabs, TabsList, TabsTrigger, TabsContent from shadcn/ui tabs
- Import PlaceholderBlock from layout
- 3 tabs: "Net Worth", "Cash Flow", "Investment Growth" (per locked decision: same 3-tab structure as PWL)
- defaultValue="net-worth"
- TabsList: `grid w-full grid-cols-3`
- Each TabsContent renders a PlaceholderBlock with appropriate label and minHeight="300px"
- These placeholders will be replaced in Phase 6 with actual charts

**Step 7: Create src/components/results/theme-toggle.tsx**

'use client' component using next-themes useTheme hook.

- Import Sun and Moon icons from lucide-react
- Import Button from shadcn/ui
- Toggle between light and dark themes on click
- Use the sun/moon icon rotation pattern from research:
  - Sun: visible in light mode, hidden in dark mode
  - Moon: hidden in light mode, visible in dark mode
  - Use `transition-all` for smooth rotation
- Button variant="ghost" size="icon"
- Include sr-only text "Toggle theme"
- Wrap in a mounted check to avoid hydration mismatch (useEffect + useState mounted pattern)
  </action>
  <verify>
    <automated>cd "d:/Coding Projects/Artificially Financially Free" && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Check that all 7 files exist and TypeScript compiles without errors</manual>
  </verify>
  <done>
    - verdict.ts exports calculateVerdict that correctly identifies rent/buy/tie from ComparisonResult
    - formatting.ts has Decimal-aware formatCurrencyDecimal and formatPercentageDecimal
    - VerdictCard renders headline + templated summary with real numbers
    - SummaryCards renders 2x2 grid with dollar advantage emphasized
    - ChartTabs renders 3-tab structure with placeholders
    - ThemeToggle switches between light/dark themes
    - tabs.tsx UI component installed
  </done>
</task>

<task type="auto">
  <name>Task 2: CostBreakdownTable, YearComparisonPanel, and full MainContent integration</name>
  <files>
    src/components/results/cost-breakdown-table.tsx
    src/components/results/year-comparison-panel.tsx
    src/components/layout/main-content.tsx
    src/app/page.tsx
    src/app/layout.tsx
  </files>
  <action>
**Step 1: Create src/components/results/cost-breakdown-table.tsx**

'use client' component accepting `results: ComparisonResult` prop.

- Import Accordion, AccordionItem, AccordionTrigger, AccordionContent from shadcn/ui accordion
- Import formatCurrencyDecimal from formatting
- Extract data from results:
  - **Initial Costs (Buyer):** From `results.housingProjection.upfrontCosts` — downPayment, cmhcPremium, cmhcPst, ltt, buyingClosingCosts, totalCashRequired
  - **Initial Costs (Renter):** $0 (or "None" — renters have no upfront costs)
  - **Recurring Annual Costs (Buyer):** From last year of `results.housingProjection.yearlyProjection` — mortgagePayment, propertyTax, maintenance, homeInsurance. Show as annual total.
  - **Recurring Annual Costs (Renter):** From last year of `results.yearlyComparison` — annualRent
  - **Opportunity Costs (Buyer):** Down payment + closing costs that could have been invested. Show total invested if renting: final portfolio value
  - **Opportunity Costs (Renter):** Foregone home equity growth. Show buyer's final home equity
  - **Net Proceeds at Sale (Buyer):** From `results.housingProjection.exitPosition` — homeValue, remainingMortgage, sellingCosts, netProceeds
  - **Net Proceeds at Sale (Renter):** Portfolio value at end (from final yearlyComparison.renterNetWorth)

- Layout: Side-by-side comparison table with 3 visual columns:
  - Column 1: Category name (left-aligned)
  - Column 2: Renter value (right-aligned)
  - Column 3: Buyer value (right-aligned)
- Use Accordion with `type="multiple"` so users can expand several rows at once
- Each AccordionItem: Trigger shows category name + total values for renter and buyer. Content shows sub-item breakdown.
- 4 main expandable rows per locked decision:
  1. Initial Costs
  2. Recurring Annual Costs (show year-1 values as representative)
  3. Opportunity Costs
  4. Net Proceeds at Sale
- Default state: all collapsed (static summary view per locked decision)
- Use `text-muted-foreground` for sub-items, regular weight for totals
- Mobile: Use `overflow-x-auto` wrapper with `min-w-[500px]` inner div for horizontal scroll on small screens (per research recommendation)

**Step 2: Create src/components/results/year-comparison-panel.tsx**

'use client' component accepting `results: ComparisonResult` prop.

- Import Slider from shadcn/ui slider
- Import Button from shadcn/ui button
- Import Card from shadcn/ui card
- Import formatCurrencyDecimal from formatting
- Import { useDeferredValue } from 'react' (for slider performance per research pitfall #6)
- State: `selectedYear` (useState, default 1)
- `deferredYear = useDeferredValue(selectedYear)` — deferred for smooth slider dragging

- **Preset buttons row** (per locked decision):
  - "Break-even Year" button — onClick sets selectedYear to results.breakEvenWithSelling (disabled if 'never')
  - "Mortgage Paid Off" button — onClick sets selectedYear to results.housingProjection.yearlyProjection.length (amortization years, capped to time horizon)
  - Use Button variant="outline" size="sm"

- **Slider**: min=1, max=yearlyComparison.length (time horizon), step=1
  - `value={[selectedYear]}`, `onValueChange={([v]) => setSelectedYear(v)}`
  - Label below: "Year {selectedYear} of {max}"

- **Metrics display for selected year** (per locked decision, 3 metrics):
  - Get year data from `results.yearlyComparison[deferredYear - 1]` (0-indexed array, 1-indexed slider)
  - Get housing data from `results.housingProjection.yearlyProjection[deferredYear - 1]`

  1. **Net Worth Comparison** (primary, largest): Renter net worth vs Buyer net worth (withSelling). Show which is ahead and by how much.
  2. **Annual Costs**: Renter annualRent vs Buyer buyerAnnualCost for that year
  3. **Cumulative Position**: Renter portfolio value vs Buyer home equity (homeEquity from housing projection)

- Layout each metric as a row with renter value on left, buyer value on right, labels above
- Use colored text to indicate which side is ahead (green for winner of each metric)
- Net worth difference as primary metric (largest font per research recommendation)

**Step 3: Update src/app/layout.tsx**

Update ThemeProvider configuration:
- Change `disableTransitionOnChange` to `disableTransitionOnChange={false}` (or remove the prop entirely since false is default) — enables smooth CSS transitions when toggling theme
- Keep `defaultTheme="light"` (light-first per current setup; dark-first from Phase 1 was updated in Phase 4 to light)
- Add `enableSystem` prop so users with system dark mode preference get dark automatically

**Step 4: Update src/app/page.tsx**

Add ThemeToggle to the header bar:
- Import ThemeToggle from `@/components/results/theme-toggle`
- Add to the header, right-aligned (use `ml-auto` or `flex justify-between` on the header inner container)
- Position: right side of the header bar, vertically centered

**Step 5: Rewrite src/components/layout/main-content.tsx**

Complete rewrite replacing ALL placeholder blocks and the temporary results summary card with proper result components.

- Keep 'use client' directive
- Keep `useDebouncedComparison` hook
- Import all result components: VerdictCard, ChartTabs, SummaryCards, CostBreakdownTable, YearComparisonPanel
- Remove PlaceholderBlock import (no longer needed in this file)
- Remove temporary Card/CardHeader/CardTitle/CardContent imports

- Layout (per locked decision scroll flow: verdict -> tabs -> summary cards -> year comparison -> cost breakdown):
  ```
  <main className="flex-1 p-6">
    <div className="mx-auto flex max-w-4xl flex-col gap-6">
      {isCalculating && <loading indicator>}
      {results && (
        <>
          <VerdictCard results={results} />
          <ChartTabs />
          <SummaryCards results={results} />
          <YearComparisonPanel results={results} />
          <CostBreakdownTable results={results} />
        </>
      )}
      {!results && !isCalculating && (
        <p className="text-center text-muted-foreground py-12">
          Adjust inputs to see your rent vs. buy comparison
        </p>
      )}
    </div>
  </main>
  ```

- Show a subtle loading state when isCalculating (e.g., `opacity-50` on results or a "Recalculating..." text)
- When no results and not calculating, show the empty state message
  </action>
  <verify>
    <automated>cd "d:/Coding Projects/Artificially Financially Free" && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -10</automated>
    <manual>Run npm run dev, verify all result sections render with live data, verify theme toggle works, verify accordion expands, verify year slider updates metrics</manual>
  </verify>
  <done>
    - CostBreakdownTable renders 4 expandable rows with renter vs buyer side-by-side values
    - YearComparisonPanel has working slider from Year 1 to time horizon with preset buttons
    - MainContent shows full results flow: verdict -> tabs -> summary cards -> year comparison -> cost breakdown
    - Theme toggle in header switches between light and dark modes with smooth transition
    - ThemeProvider updated with enableSystem and transition support
    - No placeholder blocks remain in the results area
    - All components consume live ComparisonResult data
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles with zero errors: `npx tsc --noEmit`
2. Next.js builds successfully: `npm run build`
3. All result components render with live data when inputs are provided
4. Theme toggle switches between light and dark modes without FOUC
5. Verdict shows correct decision based on final net worth comparison
6. Summary cards display correct metrics with dollar advantage emphasized
7. Cost breakdown table expands/collapses with proper sub-item details
8. Year comparison slider updates metrics smoothly with preset button navigation
</verification>

<success_criteria>
- Users see a clear rent-vs-buy verdict with dollar advantage and templated reasoning when inputs are provided
- 4 summary cards display in 2x2 grid with dollar advantage card visually prominent
- 3-tab chart structure exists (placeholders for Phase 6)
- Light/dark theme toggle works in header
- Cost breakdown table has 4 expandable categories with renter vs buyer side-by-side values
- Year comparison slider with break-even and mortgage-paid-off preset buttons shows 3 metrics per year
- All placeholder blocks in MainContent have been replaced with functional components
- Zero TypeScript errors, successful Next.js build
</success_criteria>

<output>
After completion, create `.planning/phases/05-verdict-results-display/05-01-SUMMARY.md`
</output>
