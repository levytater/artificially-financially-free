---
phase: 02-housing-cost-engine
plan: 02
type: tdd
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/lib/calculations/mortgage.ts
  - src/lib/calculations/cmhc.ts
  - __tests__/calculations/mortgage.test.ts
  - __tests__/calculations/cmhc.test.ts
autonomous: true
requirements:
  - CALC-01
  - CALC-02

must_haves:
  truths:
    - "Monthly mortgage payment matches RBC/TD calculator within $1 for representative scenarios"
    - "Canadian semi-annual compounding formula is used (not US monthly compounding)"
    - "Amortization schedule tracks principal vs interest split per month"
    - "Final mortgage payment adjusts to zero out remaining balance exactly"
    - "CMHC premium is calculated correctly across all 6 LTV tiers"
    - "Amortization surcharge of 0.20% applied for terms over 25 years"
    - "CMHC PST calculated for ON (8%), QC (9%), SK (6%) and paid separately from premium"
    - "CMHC premium added to mortgage balance, PST NOT added to mortgage"
    - "No CMHC required when down payment >= 20%"
    - "CMHC validation: purchase price > $1,499,999 with < 20% down is flagged"
  artifacts:
    - path: "src/lib/calculations/mortgage.ts"
      provides: "Monthly payment + amortization schedule using Canadian semi-annual compounding"
      exports: ["calculateMonthlyPayment", "generateAmortizationSchedule", "calculateYearlyMortgageSummary"]
    - path: "src/lib/calculations/cmhc.ts"
      provides: "CMHC insurance premium calculation with PST"
      exports: ["calculateCmhcPremium"]
    - path: "__tests__/calculations/mortgage.test.ts"
      provides: "Mortgage calculation tests with bank-verified scenarios"
      contains: "calculateMonthlyPayment"
    - path: "__tests__/calculations/cmhc.test.ts"
      provides: "CMHC tier boundary and PST tests"
      contains: "calculateCmhcPremium"
  key_links:
    - from: "src/lib/calculations/mortgage.ts"
      to: "@/lib/decimal"
      via: "Decimal.js for all arithmetic"
      pattern: "import.*Decimal.*from.*@/lib/decimal"
    - from: "src/lib/calculations/cmhc.ts"
      to: "@/lib/data/cmhc-rates"
      via: "CMHC tier lookup"
      pattern: "import.*CMHC_TIERS.*from.*cmhc-rates"
    - from: "src/lib/calculations/cmhc.ts"
      to: "@/lib/decimal"
      via: "Decimal.js for premium arithmetic"
      pattern: "import.*Decimal.*from.*@/lib/decimal"
---

<objective>
Implement and test Canadian mortgage payment calculation (semi-annual compounding) and CMHC insurance premium calculation using TDD methodology.

Purpose: These are the two most critical calculations in the housing cost engine. Mortgage payment accuracy is the #1 success criterion ("matches RBC/TD within $1/month"). CMHC has complex tier boundaries and PST rules that need thorough testing.

Output: Battle-tested mortgage and CMHC functions with comprehensive test coverage.
</objective>

<execution_context>
@C:/Users/levfe/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/levfe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-housing-cost-engine/02-RESEARCH.md
@.planning/phases/02-housing-cost-engine/02-01-SUMMARY.md

@src/lib/decimal.ts
@src/types/housing.ts
@src/lib/data/cmhc-rates.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD mortgage payment and amortization schedule</name>
  <files>
    __tests__/calculations/mortgage.test.ts
    src/lib/calculations/mortgage.ts
  </files>
  <action>
**RED phase -- Write failing tests first:**

Create `__tests__/calculations/mortgage.test.ts` with these test cases:

**calculateMonthlyPayment tests:**
1. $480,000 at 5.5% for 25 years -- expected ~$2,921/month (verify against RBC calculator before hardcoding)
2. $300,000 at 4.5% for 25 years -- verify against RBC
3. $800,000 at 6.0% for 30 years -- verify against RBC
4. $200,000 at 3.5% for 20 years -- verify against RBC
5. Edge case: 0% interest rate -- payment = principal / months
6. Tolerance: use `.toBeCloseTo(expected, 0)` for within-$1 matching (or compare with Decimal math and assert difference < 1)

To get the expected values: Manually check against RBC mortgage calculator (https://apps.royalbank.com/apps/mortgages/mortgage-payment-calculator/) or use the known formula to compute expected values. The research gives us the formula: `r_m = (1 + annualRate/2)^(1/6) - 1`, `PMT = (r_m * P) / (1 - (1 + r_m)^(-n))`.

Pre-computed expected values (using the semi-annual compounding formula):
- $480K at 5.5%/25yr: effective monthly rate = 0.004531656... PMT = ~$2,920.82
- $300K at 4.5%/25yr: effective monthly rate = 0.003711578... PMT = ~$1,651.42
- $800K at 6.0%/30yr: effective monthly rate = 0.004938622... PMT = ~$4,747.77
- $200K at 3.5%/20yr: effective monthly rate = 0.002894693... PMT = ~$1,151.90

NOTE: Verify these against an online Canadian mortgage calculator before committing the test expectations. Use the actual calculator output as the test constant. The test should assert within $1 tolerance.

**generateAmortizationSchedule tests:**
1. $480K at 5.5%/25yr produces 300 entries (25*12)
2. First month: interest portion > principal portion (early in mortgage)
3. Last month: remaining balance = $0.00 (exact payoff)
4. Sum of all principal payments = original principal
5. All monthly payments are equal (except possibly last)
6. Month 1 interest = principal * effective_monthly_rate

**calculateYearlyMortgageSummary tests:**
1. Takes amortization schedule, returns yearly aggregates
2. Year 1: sum of months 1-12 for P&I split
3. Year 25 (last): remaining balance = $0.00

Run tests -- they should all FAIL (functions don't exist yet).

**GREEN phase -- Implement minimum code to pass:**

Create `src/lib/calculations/mortgage.ts`:

`calculateMonthlyPayment(principal: Decimal, annualRatePercent: Decimal, amortizationYears: Decimal): Decimal`
- Convert annual rate to effective monthly rate using Canadian semi-annual compounding: `rm = (1 + annualRate/100/2)^(1/6) - 1`
- Apply standard annuity formula: `PMT = (rm * P) / (1 - (1 + rm)^(-n))`
- Handle edge case: if rate is 0, return `principal / totalMonths`
- Use Decimal.js for ALL arithmetic

`generateAmortizationSchedule(principal: Decimal, annualRatePercent: Decimal, amortizationYears: Decimal): MonthlyPaymentBreakdown[]`
- Calculate monthly payment using above function
- Loop through each month: interest = balance * rm, principal = payment - interest
- Final payment adjustment: if remaining balance < regular payment, adjust final payment = balance + final interest
- Ensure final remaining balance is exactly 0

`calculateYearlyMortgageSummary(schedule: MonthlyPaymentBreakdown[]): Array<{ year: number; totalPayment: Decimal; totalPrincipal: Decimal; totalInterest: Decimal; endBalance: Decimal }>`
- Aggregate monthly data into yearly summaries
- Year N covers months (N-1)*12+1 through N*12

Run tests -- they should all PASS.

**REFACTOR phase (if needed):** Clean up, add JSDoc comments, ensure consistent naming.
  </action>
  <verify>
    <automated>npx vitest run __tests__/calculations/mortgage.test.ts</automated>
    <manual>Monthly payment values match Canadian mortgage calculator output within $1</manual>
  </verify>
  <done>Mortgage payment matches bank calculators within $1 for all 4 representative scenarios. Amortization schedule pays off to exactly $0. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: TDD CMHC insurance premium calculation</name>
  <files>
    __tests__/calculations/cmhc.test.ts
    src/lib/calculations/cmhc.ts
  </files>
  <action>
**RED phase -- Write failing tests first:**

Create `__tests__/calculations/cmhc.test.ts` with these test cases:

**calculateCmhcPremium tests:**

Tier boundary tests (purchase price $600K):
1. 20% down ($120K, LTV 80%) -- no CMHC required, premium = $0, pst = $0
2. 19.99% down (LTV 80.01%) -- should trigger tier: 80.01-85% at 2.80%
3. 15% down ($90K, LTV 85%) -- rate = 2.80%, premium = $510K * 0.028 = $14,280
4. 10% down ($60K, LTV 90%) -- rate = 3.10%, premium = $540K * 0.031 = $16,740
5. 5% down ($30K, LTV 95%) -- rate = 4.00%, premium = $570K * 0.04 = $22,800

PST tests:
6. Ontario with 10% down: PST = premium * 0.08
7. Quebec with 10% down: PST = premium * 0.09
8. Saskatchewan with 10% down: PST = premium * 0.06
9. Alberta with 10% down: PST = $0 (no PST on CMHC)
10. BC with 10% down: PST = $0 (no PST on CMHC)

Amortization surcharge tests:
11. 10% down with 25yr amortization -- no surcharge
12. 10% down with 30yr amortization -- surcharge +0.20%, total rate = 3.10% + 0.20% = 3.30%

Mortgage addition test:
13. Premium is added to mortgage (totalMortgageAddition = premium)
14. PST is NOT added to mortgage (returned separately)

Validation tests:
15. Purchase price > $1,499,999 with < 20% down -- should return indicator that CMHC is not available (throw error or return a flag)
16. Down payment >= 20% for any price -- no CMHC needed

Run tests -- all FAIL.

**GREEN phase -- Implement:**

Create `src/lib/calculations/cmhc.ts`:

`calculateCmhcPremium(purchasePrice: Decimal, downPaymentPercent: Decimal, amortizationYears: Decimal, province: string): CmhcResult`
- Calculate down payment and mortgage amount
- Calculate LTV ratio
- If LTV <= 0.80 (down payment >= 20%), return zeros
- If purchasePrice > CMHC_MAX_PURCHASE_PRICE and LTV > 0.80, throw an error or return a special result indicating CMHC unavailable
- Find applicable CMHC tier from CMHC_TIERS
- Apply amortization surcharge if amortizationYears > 25
- Calculate premium = mortgageAmount * rate
- Calculate PST from CMHC_PST_RATES for applicable provinces
- Return { premium, pst, totalMortgageAddition: premium }

Import CMHC_TIERS, CMHC_AMORTIZATION_SURCHARGE, CMHC_PST_RATES, CMHC_MAX_PURCHASE_PRICE from `@/lib/data/cmhc-rates`.
Use Decimal.js for ALL arithmetic.

Run tests -- all PASS.

**REFACTOR:** Add JSDoc, ensure error messages are clear.
  </action>
  <verify>
    <automated>npx vitest run __tests__/calculations/cmhc.test.ts</automated>
    <manual>CMHC premium matches known values from CMHC.ca premium calculator</manual>
  </verify>
  <done>CMHC premium calculated correctly across all 6 LTV tiers. PST applied for ON/QC/SK only. Amortization surcharge added for >25yr. Premium added to mortgage, PST kept separate. Purchase price cap validated. All tests pass.</done>
</task>

</tasks>

<verification>
1. `npx vitest run __tests__/calculations/mortgage.test.ts` -- all mortgage tests pass
2. `npx vitest run __tests__/calculations/cmhc.test.ts` -- all CMHC tests pass
3. Monthly payment for $480K at 5.5%/25yr matches RBC calculator within $1
4. CMHC tier boundaries produce correct premiums at 5%, 10%, 15%, 19.99%, 20% down
5. PST only applies in ON, QC, SK
6. No native JS arithmetic on financial values
</verification>

<success_criteria>
- Mortgage payment passes bank calculator validation for 4+ representative scenarios
- Semi-annual compounding correctly implemented (not US monthly)
- Amortization schedule pays off to exactly $0.00
- CMHC tiers, surcharge, and PST all tested and working
- All tests follow RED-GREEN-REFACTOR TDD cycle
</success_criteria>

<output>
After completion, create `.planning/phases/02-housing-cost-engine/02-02-SUMMARY.md`
</output>
