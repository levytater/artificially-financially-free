---
phase: 03-investment-comparison-engine
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/data/income-tax-brackets.ts
  - src/lib/calculations/income-tax.ts
  - src/lib/calculations/inflation.ts
  - src/types/investment.ts
  - __tests__/calculations/income-tax.test.ts
  - __tests__/calculations/inflation.test.ts
autonomous: true
requirements:
  - CALC-13
  - CALC-14

must_haves:
  truths:
    - "Combined federal + provincial marginal tax rate is calculated correctly for any income and province"
    - "Marginal rate lookup finds the correct bracket for incomes at boundaries and mid-bracket"
    - "All 10 provinces return valid marginal rates for typical Canadian incomes ($50K-$200K)"
    - "Nominal dollar values can be deflated to real dollars using an inflation rate and year"
  artifacts:
    - path: "src/lib/data/income-tax-brackets.ts"
      provides: "Federal and all 10 provincial 2025 income tax brackets"
      contains: "FEDERAL_TAX_BRACKETS"
    - path: "src/lib/calculations/income-tax.ts"
      provides: "Combined marginal tax rate calculation"
      exports: ["calculateCombinedMarginalRate"]
    - path: "src/lib/calculations/inflation.ts"
      provides: "Nominal-to-real dollar deflation"
      exports: ["deflateToRealDollars"]
    - path: "src/types/investment.ts"
      provides: "Investment and comparison type definitions"
      contains: "ComparisonInput"
    - path: "__tests__/calculations/income-tax.test.ts"
      provides: "Income tax calculation tests"
      min_lines: 40
    - path: "__tests__/calculations/inflation.test.ts"
      provides: "Inflation deflation tests"
      min_lines: 15
  key_links:
    - from: "src/lib/calculations/income-tax.ts"
      to: "src/lib/calculations/land-transfer-tax.ts"
      via: "import calculateMarginalTax"
      pattern: "import.*calculateMarginalTax.*from.*land-transfer-tax"
    - from: "src/lib/calculations/income-tax.ts"
      to: "src/lib/data/income-tax-brackets.ts"
      via: "import bracket constants"
      pattern: "import.*FEDERAL_TAX_BRACKETS.*from.*income-tax-brackets"
---

<objective>
Create income tax estimation and inflation deflation — the two foundation utilities Phase 3 needs before portfolio growth and comparison can be built.

Purpose: CALC-13 requires auto-calculating combined federal + provincial marginal tax rate from income + province. CALC-14 requires deflating nominal values to real dollars. Both are pure functions with clear I/O — ideal for TDD.

Output: Income tax bracket data for federal + 10 provinces, combined marginal rate function, inflation deflation function, and shared investment type definitions.
</objective>

<execution_context>
@C:/Users/levfe/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/levfe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-investment-comparison-engine/03-RESEARCH.md
@src/types/housing.ts
@src/lib/calculations/land-transfer-tax.ts
@src/lib/decimal.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Income tax bracket data and investment types</name>
  <files>
    src/lib/data/income-tax-brackets.ts
    src/types/investment.ts
  </files>
  <action>
Create `src/lib/data/income-tax-brackets.ts` with:
- `FEDERAL_TAX_BRACKETS: readonly TaxBracket[]` — 5 brackets from RESEARCH.md (15% to 33%)
- `PROVINCIAL_TAX_BRACKETS: Readonly<Record<ProvinceCode, readonly TaxBracket[]>>` — all 10 provinces from RESEARCH.md
- Reuse the existing `TaxBracket` interface from `@/types/housing` (same `{ from, to, rate }` format with rate as decimal, e.g. 0.15 for 15%)
- All bracket data values verified in 03-RESEARCH.md — copy exactly
- Use `as const` for type narrowing, matching the pattern in `ltt-brackets.ts`

Create `src/types/investment.ts` with type definitions needed across Phase 3:
- `PortfolioYear` — { year, startBalance, contributions, growth, endBalance } (all Decimal)
- `YearlyComparison` — { year, renterNetWorth, buyerNetWorthWithSelling, buyerNetWorthWithoutSelling, monthlySavings, annualRent, buyerAnnualCost } (all Decimal)
- `ComparisonInput` — { purchasePrice, downPaymentPercent, mortgageRate, amortizationYears, province (ProvinceCode), timeHorizon, firstTimeBuyer, monthlyRent, rentIncreasePercent, annualIncome, investmentReturnPercent, taxRateOverride? (number|null), inflationRatePercent, propertyTaxRate?, maintenanceRate?, homeInsurance?, appreciationRate? }
- `ComparisonResult` — { housingProjection (HousingProjection), portfolio (PortfolioYear[]), yearlyComparison (YearlyComparison[]), breakEvenWithSelling (number | 'never'), breakEvenWithoutSelling (number | 'never'), marginalTaxRate (Decimal), afterTaxReturnRate (Decimal) }

All financial values in result types use Decimal. Input types use plain numbers (percentage convention matching Phase 2).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Check that income-tax-brackets.ts exports FEDERAL_TAX_BRACKETS and PROVINCIAL_TAX_BRACKETS, and investment.ts exports all listed types</manual>
  </verify>
  <done>Federal + 10 provincial bracket constants exist, all investment/comparison types defined, TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: TDD income tax marginal rate + inflation deflation</name>
  <files>
    src/lib/calculations/income-tax.ts
    src/lib/calculations/inflation.ts
    __tests__/calculations/income-tax.test.ts
    __tests__/calculations/inflation.test.ts
  </files>
  <action>
**RED phase — write failing tests first:**

Create `__tests__/calculations/income-tax.test.ts`:
- Test `calculateCombinedMarginalRate(annualIncome, province)` returns Decimal percentage
- Test cases (manually computed from bracket data):
  - $60,000 income, ON: federal bracket 20.5% + ON bracket 9.15% = 29.65%
  - $60,000 income, AB: federal 20.5% + AB 10.0% = 30.5%
  - $120,000 income, ON: federal 26.0% + ON 11.16% = 37.16%
  - $200,000 income, BC: federal 29.0% + BC 14.70% = 43.70%
  - $40,000 income, QC: federal 15.0% + QC 14.0% = 29.0%
  - $0 income: returns lowest bracket rates (federal 15% + provincial lowest)
  - $300,000 income, ON: federal 33.0% + ON 13.16% = 46.16%
- Test each province at $100,000 income to verify bracket data loads correctly (spot-check federal 20.5% + expected provincial rate)

Create `__tests__/calculations/inflation.test.ts`:
- Test `deflateToRealDollars(nominalValue, inflationRatePercent, year)` returns Decimal
- Test cases:
  - $100,000 at 2.5% inflation, year 0: returns $100,000 (no deflation)
  - $100,000 at 2.5% inflation, year 10: returns $100,000 / 1.025^10 ≈ $78,120.06
  - $500,000 at 0% inflation, year 25: returns $500,000 (no deflation)
  - $1,000,000 at 2.5% inflation, year 25: returns $1,000,000 / 1.025^25 ≈ $539,390.91

**GREEN phase — implement:**

Create `src/lib/calculations/income-tax.ts`:
- Import `calculateMarginalTax` from `@/lib/calculations/land-transfer-tax` (reuse the generic marginal bracket calculator, per STATE.md decision)
- Import `FEDERAL_TAX_BRACKETS`, `PROVINCIAL_TAX_BRACKETS` from `@/lib/data/income-tax-brackets`
- Implement `findMarginalRate(income: Decimal, brackets: readonly TaxBracket[]): Decimal` — iterate brackets from last to first, return rate of first bracket where income > bracket.from. This gives the marginal rate directly without computing total tax.
- Implement `calculateCombinedMarginalRate(annualIncome: number, province: ProvinceCode): Decimal` — find federal marginal rate + provincial marginal rate, return sum as percentage (multiply by 100)
- NOTE: We do NOT need `calculateMarginalTax` for marginal RATE lookup — that function calculates total TAX. We need a simpler `findMarginalRate` that just returns the bracket rate. However, import `calculateMarginalTax` anyway because Plan 03 may need total tax calculation. Actually, only import it if the income-tax module itself uses it. Since we only need the marginal RATE (not total tax), just implement `findMarginalRate` as a local function and do not import `calculateMarginalTax`. Keep the import path clean.

Create `src/lib/calculations/inflation.ts`:
- Implement `deflateToRealDollars(nominalValue: Decimal, inflationRatePercent: Decimal, year: number): Decimal`
- Formula: `nominalValue / (1 + inflationRate/100)^year`
- Handle year 0: return nominalValue unchanged
- Handle 0% inflation: return nominalValue unchanged (the formula handles this naturally)

Run tests, ensure all pass. Commit RED then GREEN separately per TDD convention.
  </action>
  <verify>
    <automated>npx vitest run __tests__/calculations/income-tax.test.ts __tests__/calculations/inflation.test.ts</automated>
  </verify>
  <done>All income tax tests pass (combined marginal rate correct for multiple income/province combos), all inflation tests pass (deflation formula verified), TypeScript compiles</done>
</task>

</tasks>

<verification>
1. `npx vitest run` — all Phase 2 tests (147) + new income tax + inflation tests pass
2. `npx tsc --noEmit` — TypeScript compiles with no errors
3. Income tax: $60K ON income returns 29.65% combined marginal rate
4. Income tax: all 10 provinces return reasonable rates for $100K income (25%-40% range)
5. Inflation: $100K at 2.5% for 10 years returns ~$78,120
</verification>

<success_criteria>
- Federal + 10 provincial income tax bracket data exists as typed constants
- `calculateCombinedMarginalRate` returns correct combined marginal rate for any income/province
- `deflateToRealDollars` correctly converts nominal to real dollars
- All investment/comparison TypeScript types defined for downstream plans
- All tests pass, TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/03-investment-comparison-engine/03-01-SUMMARY.md`
</output>
