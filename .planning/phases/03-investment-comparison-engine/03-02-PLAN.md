---
phase: 03-investment-comparison-engine
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/calculations/investment.ts
  - __tests__/calculations/investment.test.ts
autonomous: true
requirements:
  - CALC-09
  - CALC-12

must_haves:
  truths:
    - "After-tax investment return is correctly calculated using capital gains 50% inclusion rate"
    - "Investment portfolio grows from a lump sum initial investment compounded monthly"
    - "Monthly contributions are added to portfolio and compound alongside existing balance"
    - "Negative monthly savings are clamped to zero — no withdrawals from portfolio"
    - "Portfolio with 0% return grows only from contributions (no compounding growth)"
  artifacts:
    - path: "src/lib/calculations/investment.ts"
      provides: "After-tax return calculation and portfolio growth simulation"
      exports: ["calculateAfterTaxReturn", "calculatePortfolioGrowth"]
    - path: "__tests__/calculations/investment.test.ts"
      provides: "Investment calculation tests"
      min_lines: 60
  key_links:
    - from: "src/lib/calculations/investment.ts"
      to: "src/lib/decimal.ts"
      via: "import Decimal"
      pattern: "import.*Decimal.*from.*decimal"
---

<objective>
Build the investment portfolio growth engine — the renter's side of the rent-vs-buy comparison.

Purpose: CALC-09 requires investing renter savings (lump sum + monthly difference) with compound growth. CALC-12 requires modeling investment returns with tax implications (simplified to single portfolio with return % + tax rate %). These are pure math functions with clear I/O — ideal for TDD.

Output: After-tax return calculation and monthly-compounding portfolio growth function with year-by-year snapshots.
</objective>

<execution_context>
@C:/Users/levfe/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/levfe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-investment-comparison-engine/03-RESEARCH.md
@.planning/phases/03-investment-comparison-engine/03-CONTEXT.md
@src/lib/decimal.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD investment portfolio growth</name>
  <files>
    src/lib/calculations/investment.ts
    __tests__/calculations/investment.test.ts
  </files>
  <action>
**RED phase — write failing tests first:**

Create `__tests__/calculations/investment.test.ts` with tests for two functions:

**Tests for `calculateAfterTaxReturn`:**
- 7% nominal return, 30% marginal tax rate: `afterTax = 0.07 * (1 - 0.30 * 0.5) = 0.07 * 0.85 = 0.0595` (5.95%)
- 7% nominal return, 0% tax rate: `afterTax = 0.07 * (1 - 0 * 0.5) = 0.07` (7.0%)
- 0% nominal return, any tax rate: `afterTax = 0.0` (0%)
- 10% nominal return, 46% tax rate: `afterTax = 0.10 * (1 - 0.46 * 0.5) = 0.10 * 0.77 = 0.077` (7.7%)
- Inputs are percentages (7.0, 30.0), function returns the after-tax annual rate as a Decimal (0.0595, not 5.95)

**Tests for `calculatePortfolioGrowth`:**
- **Lump sum only, no contributions:** $100,000 lump sum, 5 years, 5.95% annual after-tax return, monthlySavings all $0. Verify year 5 endBalance matches manual calculation: $100,000 * (1.0595)^5 ≈ $133,469. Use tolerance of $5 for Decimal precision.
- **Contributions only, no lump sum:** $0 lump sum, 3 years, $1,000/month contribution (same each year), 6% after-tax return. Verify final balance against iterative month-by-month computation.
- **Mixed lump sum + contributions:** $50,000 lump sum, 3 years, $500/month, 5% return. Verify year 1 start/end balance, contributions, and growth fields are populated.
- **Zero return:** $50,000 lump sum, 2 years, $1,000/month, 0% return. End balance should equal $50,000 + $24,000 = $74,000 exactly.
- **Negative savings clamped:** Pass a year with negative monthlySavings (e.g., Decimal(-500)). The function should treat it as zero (no withdrawal). Verify balance only grows from compounding.
- **Variable yearly savings:** Array where year 1 = $1,500/month, year 2 = $1,200/month, year 3 = $0/month. Verify each year snapshot has correct contributions amount.
- **Single year:** 1-year horizon. Verify array has exactly 1 element with year=1.

**GREEN phase — implement:**

Create `src/lib/calculations/investment.ts`:

Implement `calculateAfterTaxReturn(nominalReturnPercent: Decimal, marginalTaxRatePercent: Decimal): Decimal`:
- Capital gains 50% inclusion rate (per CONTEXT.md: confirmed, proposed 66.67% increase was cancelled)
- Formula: `afterTaxRate = (nominalReturn/100) * (1 - (taxRate/100) * 0.5)`
- Returns annual after-tax rate as a decimal (e.g., 0.0595), NOT as a percentage
- Both inputs are percentages (e.g., 7.0 for 7%, 30.0 for 30%)

Implement `calculatePortfolioGrowth(initialLumpSum: Decimal, yearlyMonthlySavings: Decimal[], annualAfterTaxReturn: Decimal): PortfolioYear[]`:
- Import `PortfolioYear` from `@/types/investment`
- Monthly compounding: `monthlyRate = (1 + annualAfterTaxReturn)^(1/12) - 1`
- Month-by-month loop: compound balance first, then add contribution
- Clamp monthly savings to max(0, savings) — per CONTEXT.md: "Negative monthly savings → zero"
- Return array of `PortfolioYear` objects with year, startBalance, contributions, growth, endBalance
- Handle 0% return edge case: monthlyRate = 0, balance only grows from contributions

Run tests, ensure all pass. Commit RED then GREEN separately per TDD convention.
  </action>
  <verify>
    <automated>npx vitest run __tests__/calculations/investment.test.ts</automated>
  </verify>
  <done>All investment tests pass — after-tax return correctly applies 50% inclusion, portfolio growth handles lump sum, monthly contributions, zero return, negative savings clamping, and variable yearly savings. TypeScript compiles.</done>
</task>

</tasks>

<verification>
1. `npx vitest run` — all Phase 2 tests (147) + new investment tests pass
2. `npx tsc --noEmit` — TypeScript compiles with no errors
3. After-tax return: 7% nominal at 30% tax = 5.95% (capital gains 50% inclusion)
4. Portfolio: $100K lump sum at 5.95% for 5 years ≈ $133,469
5. Portfolio: $0 lump sum + $1,000/month at 0% for 2 years = exactly $24,000
6. Negative savings clamped to zero — portfolio never decreases
</verification>

<success_criteria>
- `calculateAfterTaxReturn` correctly applies capital gains 50% inclusion rate
- `calculatePortfolioGrowth` handles lump sum + monthly contributions with monthly compounding
- Negative monthly savings clamped to zero (no withdrawals)
- Zero return scenario works (portfolio = sum of contributions + lump sum)
- All tests pass, TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/03-investment-comparison-engine/03-02-SUMMARY.md`
</output>
