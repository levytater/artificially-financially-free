---
phase: 05.1-results-refinement-audit-trail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/info-tooltip.tsx
  - src/content/tooltips.ts
  - src/components/results/summary-cards.tsx
  - src/components/results/verdict-card.tsx
  - src/components/results/theme-toggle.tsx
  - src/app/layout.tsx
autonomous: true
requirements:
  - UX-02
  - UX-04
  - EDU-01
  - EDU-02
  - VIZ-01
  - VIZ-02

must_haves:
  truths:
    - "Every result metric (net worth, advantage, tax rate, break-even) has an info tooltip explaining what it means"
    - "Dollar advantage card is visually emphasized as the most important metric with border-2, larger text, and bg-primary/5"
    - "Verdict card shows dynamic income-based tax rate explanation (e.g. 'Based on your $80,000 income in ON...')"
    - "Theme toggle has visible text label or improved indicator styling beyond just an icon"
    - "TooltipProvider wraps the app at root level to prevent per-component hydration issues"
  artifacts:
    - path: "src/components/ui/info-tooltip.tsx"
      provides: "Reusable InfoTooltip component wrapping shadcn/ui Tooltip with info icon trigger"
      min_lines: 25
    - path: "src/content/tooltips.ts"
      provides: "Extended tooltip content for result metrics (renterNetWorth, buyerNetWorth, dollarAdvantage, percentageAdvantage, breakEvenYear, marginalTaxRate)"
    - path: "src/components/results/summary-cards.tsx"
      provides: "Enhanced summary cards with InfoTooltip on each metric and emphasized dollar advantage card"
    - path: "src/components/results/verdict-card.tsx"
      provides: "Enhanced verdict card with income-based tax rate explanation and InfoTooltips"
    - path: "src/components/results/theme-toggle.tsx"
      provides: "Improved theme toggle with visible indicator beyond icon-only"
  key_links:
    - from: "src/components/ui/info-tooltip.tsx"
      to: "src/components/ui/tooltip.tsx"
      via: "imports Tooltip, TooltipContent, TooltipTrigger from shadcn/ui"
      pattern: "import.*from.*@/components/ui/tooltip"
    - from: "src/components/results/summary-cards.tsx"
      to: "src/content/tooltips.ts"
      via: "imports tooltip content for result metric descriptions"
      pattern: "import.*tooltips.*from.*@/content/tooltips"
    - from: "src/app/layout.tsx"
      to: "src/components/ui/tooltip.tsx"
      via: "wraps app in TooltipProvider"
      pattern: "TooltipProvider"
---

<objective>
Create reusable InfoTooltip component, add explanatory tooltips to all result metrics, enhance visual hierarchy of summary cards (especially dollar advantage emphasis), add income-based tax explanation to verdict, and improve theme toggle visibility.

Purpose: Users should understand every metric shown in the results without needing external reference. The dollar advantage card should immediately draw attention as the most important number. The theme toggle should be discoverable without guessing.

Output: InfoTooltip component, extended tooltip content, enhanced summary-cards.tsx, enhanced verdict-card.tsx, improved theme-toggle.tsx, TooltipProvider in layout.tsx
</objective>

<execution_context>
@C:/Users/levfe/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/levfe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-verdict-results-display/05-01-SUMMARY.md
@.planning/phases/05.1-results-refinement-audit-trail/05.1-RESEARCH.md
@src/components/ui/tooltip.tsx
@src/content/tooltips.ts
@src/components/results/summary-cards.tsx
@src/components/results/verdict-card.tsx
@src/components/results/theme-toggle.tsx
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create InfoTooltip component, extend tooltip content, and add TooltipProvider to layout</name>
  <files>
    src/components/ui/info-tooltip.tsx
    src/content/tooltips.ts
    src/app/layout.tsx
  </files>
  <action>
**1. Create `src/components/ui/info-tooltip.tsx`:**

Create a reusable InfoTooltip component that wraps shadcn/ui Tooltip primitives with an info icon (lucide-react `Info`). Props:
- `content: string` — tooltip text
- `side?: 'top' | 'bottom' | 'left' | 'right'` (default `'top'`)
- `className?: string` — additional classes on the trigger button

Implementation details:
- Use `<button type="button">` as trigger (not a div) for keyboard accessibility
- Set `aria-label="More information"` on the trigger
- Icon size: `h-3.5 w-3.5` inside a `h-4 w-4` button area
- Style trigger: `inline-flex items-center justify-center rounded-full text-muted-foreground hover:text-foreground transition-colors`
- Content: `max-w-xs` width, `text-xs leading-relaxed` text
- Use `collisionPadding={8}` on TooltipContent to prevent overflow on mobile
- Do NOT wrap in its own TooltipProvider — we'll add a single provider at the app root

**2. Extend `src/content/tooltips.ts`:**

Add new entries to the `tooltips` Record for result metrics. Keep the existing `TooltipContent` interface and all existing entries. Add these new keys:

- `renterNetWorth`: "The total value of the renter-investor's portfolio at the end of your time horizon. This includes the initial lump sum (what you would have spent on a down payment and closing costs) plus all monthly savings invested over time, with after-tax returns compounded monthly."
- `buyerNetWorth`: "The homeowner's net position at the end of your time horizon after selling. Calculated as: home value (with appreciation) minus remaining mortgage balance minus selling costs (realtor commission + legal fees, typically ~6% of sale price)."
- `dollarAdvantage`: "The absolute dollar difference in net worth between the winning and losing strategy at the end of your time horizon. This is the key bottom-line number — how much more wealth the better option produces."
- `percentageAdvantage`: "The dollar advantage expressed as a percentage of the winning strategy's final net worth. Helps contextualize whether the dollar difference is significant relative to the total wealth involved."
- `breakEvenYear`: "The first year where buying's net worth surpasses renting's net worth (or vice versa). Before this year, the other strategy is winning. If shown as 'Never', the winning strategy maintains its lead for the entire time horizon."
- `marginalTaxRate`: "Your combined federal + provincial marginal tax rate, auto-calculated from your annual income and province. This rate determines how much tax you pay on investment capital gains in the renting scenario. Capital gains use a 50% inclusion rate, so only half the gain is taxed at this rate."

**3. Update `src/app/layout.tsx`:**

Add `TooltipProvider` from `@/components/ui/tooltip` wrapping the children inside the existing provider hierarchy. Place it inside `CalculatorProvider` (innermost position):
```
ThemeProvider -> NuqsAdapter -> CalculatorProvider -> TooltipProvider -> {children}
```
Set `delayDuration={200}` on TooltipProvider for responsive tooltip display.
  </action>
  <verify>
    <automated>cd "d:/Coding Projects/Artificially Financially Free" && npx tsc --noEmit</automated>
    <manual>Verify info-tooltip.tsx exports InfoTooltip, tooltips.ts has 6 new result metric entries, layout.tsx has TooltipProvider</manual>
  </verify>
  <done>InfoTooltip component exists and exports correctly, tooltips.ts has entries for all 6 result metrics, TooltipProvider wraps the app at root level</done>
</task>

<task type="auto">
  <name>Task 2: Enhance summary cards, verdict card, and theme toggle with tooltips and visual hierarchy</name>
  <files>
    src/components/results/summary-cards.tsx
    src/components/results/verdict-card.tsx
    src/components/results/theme-toggle.tsx
  </files>
  <action>
**1. Enhance `src/components/results/summary-cards.tsx`:**

Add InfoTooltip next to each metric label. Import `InfoTooltip` from `@/components/ui/info-tooltip` and `tooltips` from `@/content/tooltips`.

For each of the 4 cards, add `<InfoTooltip content={tooltips.KEY.description} />` next to the label text. Wrap each label in a flex container: `<div className="flex items-center gap-1.5">`.

Keys: `renterNetWorth`, `buyerNetWorth`, `dollarAdvantage`, `percentageAdvantage`.

Enhance the Dollar Advantage card (3rd card) visual emphasis:
- Change card className from `border-primary bg-primary/5` to `border-2 border-primary bg-primary/5`
- Increase the value text from `text-2xl` to `text-3xl`
- Add a subtle colored left border or glow effect to draw eye

**2. Enhance `src/components/results/verdict-card.tsx`:**

Add a tax rate explanation section below the existing verdict text. Import `useCalculator` from `@/providers/calculator-provider` to access the current input state (province, annualIncome).

After the break-even text, add a new paragraph showing:
"Tax rate: {marginalTaxRate}% — Based on your ${annualIncome} annual income in {provinceName}, your combined federal + provincial marginal rate affects taxation of investment gains as a renter."

Use `formatCurrencyDecimal` for income, `formatPercentageDecimal` for tax rate. Access `results.marginalTaxRate` which is already available in ComparisonResult.

Get the province display name from the existing `PROVINCES` data (import from `@/data/provinces`). Use `InfoTooltip` with `tooltips.marginalTaxRate.description` next to the tax rate label.

Also add `InfoTooltip` with `tooltips.breakEvenYear.description` next to break-even year text.

**3. Enhance `src/components/results/theme-toggle.tsx`:**

Improve toggle visibility beyond icon-only. Add a visible text label next to the icon:
- Change from `size="icon"` to `size="sm"` on the Button
- Add text label: "Light" when in dark mode (showing what you'll switch to), "Dark" when in light mode
- Keep the existing sun/moon icon animation
- Add `gap-2` between icon and text
- Ensure `aria-label` and `title` remain descriptive
- Add `className="hover:bg-accent"` if not already present
  </action>
  <verify>
    <automated>cd "d:/Coding Projects/Artificially Financially Free" && npx tsc --noEmit && npm run build</automated>
    <manual>Verify each summary card has an info icon next to its label, dollar advantage card has border-2 and text-3xl, verdict card shows marginal tax rate with explanation, theme toggle shows text label</manual>
  </verify>
  <done>All 4 summary cards have InfoTooltip icons, dollar advantage card is visually emphasized with border-2 and text-3xl, verdict card includes income-based tax rate explanation, theme toggle has visible text label</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero TypeScript errors
2. `npm run build` — successful Next.js production build
3. InfoTooltip component renders accessible tooltip on hover/focus
4. All 4 summary card labels have info icons
5. Dollar advantage card stands out visually from other cards
6. Verdict card shows marginal tax rate with income/province explanation
7. Theme toggle shows "Light"/"Dark" text label next to icon
8. No hydration warnings in browser console (single TooltipProvider at root)
</verification>

<success_criteria>
- InfoTooltip component is reusable and accessible (keyboard, screen reader)
- Every result metric has an explanatory tooltip
- Dollar advantage card is the most visually prominent card
- Users understand where the marginal tax rate comes from
- Theme toggle is discoverable with text label
- Zero TypeScript or build errors
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-results-refinement-audit-trail/05.1-01-SUMMARY.md`
</output>
