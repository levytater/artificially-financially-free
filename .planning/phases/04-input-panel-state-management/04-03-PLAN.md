---
phase: 04-input-panel-state-management
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/hooks/use-wizard-state.ts
  - src/components/input-panel/wizard-modal.tsx
  - src/components/input-panel/wizard-step-1.tsx
  - src/components/input-panel/wizard-step-2.tsx
  - src/components/input-panel/wizard-step-3.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - INPUT-01
  - INPUT-02
  - INPUT-03
  - INPUT-04
  - INPUT-05
  - INPUT-09
  - INPUT-10

must_haves:
  truths:
    - "First-time visitor sees a modal wizard overlay before any calculator content"
    - "Wizard has 3 steps with Back/Next navigation and progress dots"
    - "Step 1 collects province, annual income, and first-time buyer checkbox"
    - "Step 2 collects purchase price, down payment %, mortgage rate, and amortization"
    - "Step 3 collects monthly rent, expected investment return, time horizon, and Simple/Advanced mode choice"
    - "Completing the wizard persists completion state to localStorage so it never shows again"
    - "Each wizard step has an educational paragraph explaining why these inputs matter"
  artifacts:
    - path: "src/hooks/use-wizard-state.ts"
      provides: "Wizard step management with localStorage persistence"
      exports: ["useWizardState"]
    - path: "src/components/input-panel/wizard-modal.tsx"
      provides: "Modal container with step routing and navigation"
      exports: ["WizardModal"]
    - path: "src/components/input-panel/wizard-step-1.tsx"
      provides: "Step 1: Your Situation inputs"
      exports: ["WizardStep1"]
    - path: "src/components/input-panel/wizard-step-2.tsx"
      provides: "Step 2: The Home inputs"
      exports: ["WizardStep2"]
    - path: "src/components/input-panel/wizard-step-3.tsx"
      provides: "Step 3: The Alternative inputs + mode toggle"
      exports: ["WizardStep3"]
  key_links:
    - from: "src/components/input-panel/wizard-modal.tsx"
      to: "src/hooks/use-wizard-state.ts"
      via: "useWizardState hook import"
      pattern: "import.*useWizardState.*from.*use-wizard-state"
    - from: "src/components/input-panel/wizard-step-1.tsx"
      to: "src/providers/calculator-provider.tsx"
      via: "useCalculator hook for reading/writing state"
      pattern: "import.*useCalculator.*from.*calculator-provider"
    - from: "src/app/page.tsx"
      to: "src/components/input-panel/wizard-modal.tsx"
      via: "WizardModal rendered in page"
      pattern: "import.*WizardModal.*from.*wizard-modal"
---

<objective>
Build the first-visit 3-step wizard modal that guides users through initial calculator setup with educational context at each step. Include a Simple/Advanced mode question in Step 3.

Purpose: The wizard is the user's first interaction with the calculator. It educates about WHY each input matters (per CONTEXT.md: "each step tells you WHY these numbers matter before asking for them") and collects core inputs before revealing the full sidebar.

Output: WizardModal component mounted in page.tsx, useWizardState hook with localStorage persistence, and 3 step components using the reusable input components from Plan 02.
</objective>

<execution_context>
@C:/Users/levfe/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/levfe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-input-panel-state-management/04-CONTEXT.md
@.planning/phases/04-input-panel-state-management/04-RESEARCH.md
@.planning/phases/04-input-panel-state-management/04-01-SUMMARY.md
@.planning/phases/04-input-panel-state-management/04-02-SUMMARY.md
@src/types/calculator.ts
@src/providers/calculator-provider.tsx
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useWizardState hook and WizardModal container</name>
  <files>
    src/hooks/use-wizard-state.ts
    src/components/input-panel/wizard-modal.tsx
  </files>
  <action>
**Create `src/hooks/use-wizard-state.ts`:**
- `'use client'` directive
- Import `useState` from React and `useLocalStorageState` from `use-local-storage-state`
- localStorage key: `'aff-wizard-completed'` with `defaultValue: false`
- State: `currentStep` (1-3, local useState), `hasCompletedWizard` (persisted boolean)
- Expose: `{ hasCompletedWizard, currentStep, nextStep, prevStep, completeWizard, isFirstStep, isLastStep, resetWizard }`
  - `nextStep`: `setCurrentStep(s => Math.min(s + 1, 3))`
  - `prevStep`: `setCurrentStep(s => Math.max(s - 1, 1))`
  - `completeWizard`: `setHasCompletedWizard(true)`
  - `resetWizard`: `setHasCompletedWizard(false); setCurrentStep(1)` (for testing/debug)
  - `isFirstStep`: `currentStep === 1`
  - `isLastStep`: `currentStep === 3`

**Create `src/components/input-panel/wizard-modal.tsx`:**
- `'use client'` directive
- Install shadcn/ui dialog if not already: `npx shadcn@latest add dialog --yes`
- Import `Dialog`, `DialogContent`, `DialogHeader`, `DialogTitle`, `DialogDescription` from `@/components/ui/dialog`
- Import `Button` from `@/components/ui/button`
- Import `useWizardState` from `@/hooks/use-wizard-state`
- Import WizardStep1, WizardStep2, WizardStep3 (to be created in Task 2)

Implementation:
- If `hasCompletedWizard` is true, return `null` (don't render)
- Render shadcn `Dialog` with `open={!hasCompletedWizard}` and NO `onOpenChange` handler (prevent closing by clicking outside -- user must complete the wizard)
- `DialogContent` with `className="max-w-2xl"` and the `onInteractOutside` handler set to `(e) => e.preventDefault()` to prevent closing on outside click. Also add `onEscapeKeyDown={(e) => e.preventDefault()}` to prevent Escape key closing.
- Hide the default close button by adding `[&>button]:hidden` class or using the `hideCloseButton` approach (check shadcn Dialog API -- may need to not render DialogClose, or override with CSS `[&>button:last-child]:hidden` on DialogContent)
- `DialogHeader` with title "Rent vs. Buy Calculator" and subtitle "Step {currentStep} of 3"
- Step content area: conditionally render WizardStep1/2/3 based on currentStep
- Navigation footer:
  - Left: "Back" button (`variant="outline"`, disabled when `isFirstStep`), onClick `prevStep`
  - Right: "Next" button when not last step, "See My Results" button (primary, with right arrow icon) when last step. Last step calls `completeWizard`
- Progress dots: 3 small circles below navigation. Active step = `bg-primary`, inactive = `bg-muted`. Use `div` with `h-2 w-2 rounded-full` classes.

Layout considerations:
- Dialog should be centered with adequate padding
- Step content area should have consistent min-height (~300px) to prevent layout shifts between steps
- Navigation buttons should be at the bottom, separated from step content
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Verify useWizardState hook and WizardModal component exist with correct structure</manual>
  </verify>
  <done>
- useWizardState manages step (1-3) and completion state in localStorage
- WizardModal renders as unclosable dialog when wizard not completed
- Step routing renders correct step component based on currentStep
- Back/Next navigation works with proper disable states
- Progress dots indicate current step visually
- Dialog hidden after wizard completion
  </done>
</task>

<task type="auto">
  <name>Task 2: Create wizard steps 1-3 and mount WizardModal in page</name>
  <files>
    src/components/input-panel/wizard-step-1.tsx
    src/components/input-panel/wizard-step-2.tsx
    src/components/input-panel/wizard-step-3.tsx
    src/app/page.tsx
  </files>
  <action>
All 3 step components follow the same pattern:
- `'use client'` directive
- Import `useCalculator` from `@/providers/calculator-provider`
- Import relevant input components from `@/components/input-panel/`
- Import validation functions from `@/lib/validation`
- Each step has an educational intro paragraph (2-3 sentences) at the top explaining WHY these inputs matter, styled as muted text. Include an expandable "Learn more" section using a `details`/`summary` HTML element (no library needed -- native HTML is sufficient and accessible) with deeper educational content.

**Create `src/components/input-panel/wizard-step-1.tsx` -- "Your Situation":**

Educational intro: "Where you live and how much you earn determines the taxes you'll pay on both buying a home and investing. These numbers help us calculate province-specific land transfer taxes and the marginal tax rate on your investment gains."

Learn more content: Brief explanation of how province affects LTT and tax rates, and why income matters for investment returns.

Inputs (using reusable components from Plan 02):
1. `ProvinceSelector` -- value: `state.province`, onChange updates province
2. `CurrencyInput` -- label: "Annual Gross Income", value: `state.annualIncome`, step: $5000, min: 0, max: 10,000,000
3. `CheckboxInput` -- label: "First-Time Home Buyer", description: "May qualify for land transfer tax rebates and other benefits", checked: `state.firstTimeBuyer`

Use `validateAnnualIncome` for income validation on blur.

**Create `src/components/input-panel/wizard-step-2.tsx` -- "The Home":**

Educational intro: "These numbers define the home you'd buy. The purchase price and down payment determine your mortgage size, and whether you'll need CMHC insurance (required with less than 20% down). A larger down payment means less mortgage interest but also less cash available to invest."

Learn more content: Brief explanation of CMHC insurance trigger at 20%, Canadian down payment minimums, and semi-annual compounding.

Inputs:
1. `CurrencyInput` -- label: "Purchase Price", value: `state.purchasePrice`, step: $10,000 (per CONTEXT.md: "$10,000 per click"), min: 0, max: 10,000,000
2. `PercentageInput` -- label: "Down Payment", value: `state.downPaymentPercent`, step: 1 (1% per click for down payment, not 0.10% -- down payment is a large percentage), min: 5, max: 100
3. `PercentageInput` -- label: "Mortgage Rate", value: `state.mortgageRate`, step: 0.10 (10 basis points), min: 0, max: 20
4. `StepperInput` -- label: "Amortization Period", value: `state.amortizationYears`, step: 1 (1 year per CONTEXT.md), min: 1, max: 30, suffix: "years", formatDisplay as whole number

Use `validatePurchasePrice`, `validateDownPaymentPercent`, `validateMortgageRate`, `validateAmortization` for validation on blur (track touched state per field; only show errors for touched fields).

**Create `src/components/input-panel/wizard-step-3.tsx` -- "The Alternative":**

Educational intro: "Instead of buying, you could rent and invest the difference. This step defines your rental costs and investment assumptions. The money you'd save by renting (including the down payment you'd keep) grows in the market — and that's the heart of the rent-vs-buy comparison."

Learn more content: Brief explanation of opportunity cost concept -- the down payment alone invested at 6-7% can grow significantly over 25 years. Mention the three account types (TFSA, RRSP, Non-registered) available in Advanced mode.

Inputs:
1. `CurrencyInput` -- label: "Monthly Rent", value: `state.monthlyRent`, step: $50 (per CONTEXT.md: "$50 per click"), min: 0, max: 50,000
2. `PercentageInput` -- label: "Expected Investment Return", value: `state.investmentReturn`, step: 0.10, min: 0, max: 50
3. `TimeHorizonInput` -- value: `state.timeHorizon`, onChange updates timeHorizon

Then, below the inputs, add the Simple/Advanced mode choice (per CONTEXT.md: "Offered in the wizard: 'How detailed do you want to get?'"):
- A visual toggle section with a question: "How detailed do you want to get?"
- Two option cards (not just a switch):
  - **Simple** (default): "One investment return rate, standard cost assumptions. Best for a quick comparison." -- icon: a simple/clean icon
  - **Advanced**: "Fine-tune per-account returns (TFSA, RRSP, Non-reg), maintenance costs, selling costs, and more." -- icon: a settings/gear icon
- Selecting either sets `state.advancedMode` to false or true
- Use a visually distinct selected state (border-primary + bg-primary/10 for selected, border-border for unselected)

Use `validateMonthlyRent`, `validatePercentageRate` (for investment return), `validateTimeHorizon` for validation on blur.

**Update `src/app/page.tsx`:**
- Import `WizardModal` from `@/components/input-panel/wizard-modal`
- Add `<WizardModal />` as the first child inside the outer div, before the header. The wizard renders as a Dialog overlay so position doesn't matter much, but placing it at the top of the JSX tree is cleanest.

The page structure should be:
```tsx
<div className="flex min-h-screen flex-col">
  <WizardModal />
  <header>...</header>
  <div className="flex flex-1 ...">
    <AppSidebar />
    <MainContent />
  </div>
</div>
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Run `npm run dev` and verify wizard appears on first visit, all 3 steps render inputs, navigation works, and completing wizard hides it</manual>
  </verify>
  <done>
- WizardStep1 renders ProvinceSelector, income input, and first-time buyer checkbox with educational intro
- WizardStep2 renders purchase price, down payment, mortgage rate, and amortization with educational intro
- WizardStep3 renders monthly rent, investment return, time horizon, and Simple/Advanced mode choice with educational intro
- Each step validates inputs on blur with touched-field tracking
- WizardModal is mounted in page.tsx and appears on first visit
- Completing wizard persists to localStorage and hides the modal
- All wizard inputs update calculator state via useCalculator()
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run dev` starts the dev server
3. First visit shows wizard modal (cannot be dismissed without completing)
4. All 3 steps render their respective inputs
5. Back/Next navigation works, progress dots update
6. "See My Results" on step 3 completes wizard and hides modal
7. Refreshing page after completion does NOT show wizard again (localStorage persistence)
8. Clearing localStorage (`localStorage.removeItem('aff-wizard-completed')`) re-shows wizard
</verification>

<success_criteria>
- Wizard modal appears on first visit and blocks interaction with background
- 3 steps with educational content guide user through initial setup
- All wizard inputs update calculator state in real-time
- Simple/Advanced mode choice is presented clearly in Step 3
- Wizard completion is persisted so it only shows once
- Validation errors appear only after user interacts with a field (not on mount)
</success_criteria>

<output>
After completion, create `.planning/phases/04-input-panel-state-management/04-03-SUMMARY.md`
</output>
